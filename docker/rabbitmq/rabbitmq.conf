# Topology (exchanges/queues/bindings) and users are declared as code.
# See definitions.json — it is the single source of truth for the broker layout.
load_definitions = /etc/rabbitmq/definitions.json
default_vhost = /

# Reasonable dev limits
listeners.tcp.default = 5672
management.tcp.port = 15672
